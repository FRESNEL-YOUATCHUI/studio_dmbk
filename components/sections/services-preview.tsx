'use client';
import Link from 'next/link';
import { motion } from 'framer-motion';
import { ArrowUpRight } from 'lucide-react';
import type { Service } from '@/lib/data';
export function ServicesPreview({services}:{services:Service[]}){return <section className="py-24 md:py-32 px-6 bg-white"><div className="container mx-auto"><div className="grid md:grid-cols-2 gap-8 items-end mb-16"><div><p className="eyebrow mb-4">Services</p><h2 className="font-dusan text-4xl md:text-6xl text-[#00b4d8] leading-[1.05]">Notre expertise,<br/>sans superflu.</h2></div><p className="text-sm leading-relaxed text-black/55 max-w-sm md:justify-self-end">Des offres flexibles pour bâtir une présence claire, cohérente et mémorable.</p></div><div className="grid md:grid-cols-2 gap-px bg-[#1a132d]/15 border border-[#1a132d]/15">{services.map((service,i)=><motion.article key={service.slug} initial={{opacity:0,y:25}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="group bg-white p-7 md:p-10 min-h-64 flex flex-col"><span className="text-[10px] tracking-widest text-[#eb593b]">0{i+1}</span><h3 className="text-2xl mt-7">{service.title}</h3><p className="text-sm text-black/50 mt-4 max-w-md">{service.shortDescription}</p><Link href={`/services/${service.slug}`} className="mt-auto pt-8 inline-flex items-center gap-2 text-sm">Découvrir <ArrowUpRight size={16}/></Link></motion.article>)}</div></div></section>}
